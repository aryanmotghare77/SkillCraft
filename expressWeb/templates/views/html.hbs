<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>SkillsCraft</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>

<body class="bg-gray-100">

    <!-- Navbar -->
    {{>navbar}}

    <div class="flex">

        <!-- Sidebar -->
        {{>html_sidebar}}

        <!-- Main Content -->
        <div class="ml-0 md:ml-64 w-full">
            <div class="container mx-auto p-6">

                <!-- Section: Core Concepts of HTML -->
                <section class="mb-12">
                    <h1 class="text-3xl font-bold mb-6">Introduction</h1>

                    <!-- Concept 1: Introduction to HTML -->
                    <div class="mb-8">
                        <h2 class="text-2xl font-semibold mb-4">Introduction to HTML</h2>
                        <p class="text-gray-700 mb-4">
                            HTML (Hypertext Markup Language) is the standard language for creating web pages. With HTML you can create your own website. HTML is easy to learn - You will enjoy it!
                        </p>
                        <pre class="bg-gray-100 p-4 rounded-lg border border-gray-300 mb-4">
&lt;!DOCTYPE html&gt;
&lt;html&gt;
  &lt;head&gt;
    &lt;title&gt;My First Page&lt;/title&gt;
  &lt;/head&gt;
  &lt;body&gt;
    &lt;h1&gt;Hello World!&lt;/h1&gt;
  &lt;/body&gt;
&lt;/html&gt;
                        </pre>
                    </div>

                    <!-- Concept 2: Basic HTML Structure -->
                    <div class="mb-8">
                        <h2 class="text-2xl font-semibold mb-4">Basic HTML Structure</h2>
                        <p class="text-gray-700 mb-4">
                            Every HTML document follows a basic structure. It contains a `doctype` declaration, followed by the `html`, `head`, and `body` tags. 
                        </p>
                        <pre class="bg-gray-100 p-4 rounded-lg border border-gray-300 mb-4">
&lt;!DOCTYPE html&gt;
&lt;html&gt;
  &lt;head&gt;
    &lt;title&gt;My First HTML Structure&lt;/title&gt;
  &lt;/head&gt;
  &lt;body&gt;
    &lt;p&gt;This is the body content.&lt;/p&gt;
  &lt;/body&gt;
&lt;/html&gt;
                        </pre>
                    </div>
                </section>

                <!-- HTML Code Editor Section -->
                 <div class="grid grid-cols-1 md:grid-cols-2 gap-6">
      <!-- Input Section -->
      <div>
        <h2 class="text-2xl font-semibold mb-4">Editor</h2>
        <textarea id="html-code" class="w-full h-64 p-4 text-gray-700 bg-gray-100 border border-gray-300 rounded-lg resize-none focus:outline-none focus:border-blue-500"
          placeholder="Write your HTML code here...">&lt;h1&gt;Hello, World!&lt;/h1&gt;&lt;p&gt;This is a simple HTML editor.&lt;/p&gt;</textarea>

        <div class="flex justify-end mt-4">
          <button id="run-btn" class="bg-blue-600 text-white px-4 py-2 rounded-lg hover:bg-blue-500">Run Code</button>
          <button id="clear-btn" class="bg-red-600 text-white px-4 py-2 ml-2 rounded-lg hover:bg-red-500">Clear Code</button>
        </div>
      </div>

       <!-- Output Section -->
      <div>
        <h2 class="text-2xl font-semibold mb-4">Output</h2>
        <iframe id="output-frame" class="w-full h-64 bg-white border border-gray-300 rounded-lg"></iframe>
      </div>
    </div>

            </div>
        </div>
    </div>

     <!-- JavaScript to Handle Sidebar Toggle and HTML Editor -->
     <script>
    const htmlCodeArea = document.getElementById('html-code');
    const outputFrame = document.getElementById('output-frame');
    const runButton = document.getElementById('run-btn');
    const clearButton = document.getElementById('clear-btn');

    // Function to update the iframe content with the HTML code
    function updateOutput() {
      const outputDocument = outputFrame.contentDocument || outputFrame.contentWindow.document;
      outputDocument.open();
      outputDocument.write(htmlCodeArea.value);
      outputDocument.close();
    }

    // Run code on button click
    runButton.addEventListener('click', updateOutput);

    // Clear the editor and iframe output
    clearButton.addEventListener('click', function () {
      htmlCodeArea.value = '';
      updateOutput();
    });

    // Initial run on page load
    window.onload = updateOutput;
  </script>

    </body>
    </html>