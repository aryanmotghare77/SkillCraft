<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>SkillsCraft</title>
  <script src="https://cdn.tailwindcss.com"></script>
</head>

<body class="bg-gray-100">

  <!-- Navbar -->
  {{>navbar}}

  <div class="flex">

    <!-- Sidebar -->
    {{>html_sidebar}}

    <!-- Main Content -->
    <div class="ml-0 md:ml-64 w-full">
      <div class="container mx-auto p-6">

        <!-- Section: Core Concepts of HTML -->
        <section class="mb-12">
          <h1 class="text-3xl font-bold mb-6">HTML Div</h1>

          <!-- Concept 1: Introduction to HTML -->
          <div class="mb-8">
          <h2 class="text-2xl font-semibold mb-4">Define an HTML Div</h2>
          <p class="text-gray-700 mb-4">
           A &lt;div&gt; section in a document that is styled with CSS:
          </p>
          <details>
           <summary>More About div tag</summary>
The &lt;div&gt; tag in HTML is a block-level container element used to group together sections of content or other HTML elements. It's one of the most commonly used tags for creating layouts and structuring a webpage. The <div> tag itself doesn’t inherently add any styling or behavior to its contents, but it allows developers to apply CSS styles and JavaScript functionality to the grouped content.
            
          </details>
          <pre id="code-block"class="bg-gray-100 p-4 rounded-lg border border-gray-300 mb-4">
&lt;!DOCTYPE html&gt;
&lt;html&gt;
&lt;head&gt;
&lt;style&gt;
.myDiv {
  border: 5px outset red;
  background-color: lightblue;    
  text-align: center;
}
&lt;/style&gt;
&lt;/head&gt;
&lt;body&gt;

&lt;h1&gt;The div element&lt;/h1&gt;

&lt;div class="myDiv"&gt;
  &lt;h2&gt;This is a heading in a div element&lt;/h2&gt;
  &lt;p&gt;This is some text in a div element.&lt;/p&gt;
&lt;/div&gt;

&lt;p&gt;This is some text outside the div element.&lt;/p&gt;

&lt;/body&gt;
&lt;/html&gt;

                        </pre>
                         <button id="copy-button" class="absolute bg-blue-600 text-white py-1 px-3 rounded hover:bg-blue-500 focus:outline-none focus:ring focus:ring-blue-300">Copy </button>
      
                     
      </div>
<br>
      <!-- Concept 2:  HTML DOCTYPE -->
    
      <br>
      <br>
      <!-- Concept 3: Basic HTML Heading -->
      <div class="mt-6 mb-8">
        <h2 class="text-2xl font-semibold mb-4">A Style example</h2>
        <p class="text-gray-700 mb-4">
       This example creates a shadowed box by applying a style to the <div> using CSS. Note the use of the class attribute on the &lt;div&gt; to apply the style named "shadowbox" to the element.</p> 
        <pre id="code-block1"class="bg-gray-100 p-4 rounded-lg border border-gray-300 mb-4 ml-6 mr-6">
&lt;!DOCTYPE html&gt;
&lt;html&gt;
&lt;style&gt;
.shadowbox {
  width: 15em;
  border: 1px solid #333;
  box-shadow: 8px 8px 5px #444;
  padding: 8px 12px;
  background-image: linear-gradient(180deg, #fff, #ddd 40%, #ccc);
}
&lt;/style&gt;
&lt;body&gt;

&lt;div class="shadowbox"&gt;
  &lt;p&gt;Here's a very interesting note displayed in a lovely shadowed box.&lt;/p&gt;
&lt;div&gt;
&lt;/body&gt;
&lt;/html&gt;

                        </pre>
     <button id="copy-button1" class="absolute bg-blue-600 text-white py-1 px-3 rounded hover:bg-blue-500 focus:outline-none focus:ring focus:ring-blue-300">Copy </button>
                    
                     
      </div>
      <br>
      </section>

      <!-- HTML Code Editor Section -->
      {{>editor}}

    </div>
  </div>
  </div>


{{!-- {{>footer}} --}}
  <!-- JavaScript to Handle Sidebar Toggle and HTML Editor -->
  <script>
         document.getElementById('copy-button').addEventListener('click', function () {
            const codeBlock = document.getElementById('code-block').innerText;
            navigator.clipboard.writeText(codeBlock).then(() => {
                alert('Code copied to clipboard!');
            }).catch(err => {
                console.error('Error copying code: ', err);
            });
        });
         document.getElementById('copy-button1').addEventListener('click', function () {
            const codeBlock = document.getElementById('code-block1').innerText;
            navigator.clipboard.writeText(codeBlock).then(() => {
                alert('Code copied to clipboard!');
            }).catch(err => {
                console.error('Error copying code: ', err);
            });
        });
         document.getElementById('copy-button2').addEventListener('click', function () {
            const codeBlock = document.getElementById('code-block2').innerText;
            navigator.clipboard.writeText(codeBlock).then(() => {
                alert('Code copied to clipboard!');
            }).catch(err => {
                console.error('Error copying code: ', err);
            });
        });
  </script>

</body>

</html>