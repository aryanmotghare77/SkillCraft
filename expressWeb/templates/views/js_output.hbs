<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>SkillsCraft</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <style>
       body {
            font-family: Arial, sans-serif;
            background-color: #f4f4f4;
            padding: 20px;
        }

        pre {
            background-color: #2d2d2d;
            color: #f8f8f2;
            padding: 15px;
            border-radius: 5px;
            overflow-x: auto;
            max-width: 800px;
            white-space: pre-wrap;
            word-wrap: break-word;
        }
         .keyword { color: #66d9ef; }
        .function { color: #a6e22e; }
        .string { color: #e6db74; }
        .comment { color: #75715e; }
        .number { color: #ae81ff; }

        
        </style>
</head>

<body class="bg-gray-100">

    <!-- Navbar -->
    {{>navbar}}

    <div class="flex">

        <!-- Sidebar -->
        {{>js_sidebar}}

        <!-- Main Content -->
        <div class="ml-0 md:ml-64 w-full">
            <div class="container mx-auto p-6">

                <!-- Section: Core Concepts of HTML -->
                <section class="mb-12">
                    <h1 class="text-3xl font-bold mb-6">Output</h1>

                    <!-- Concept 1: Introduction to HTML -->
                    <div class="mb-8">
                        <h2 class="text-2xl font-semibold mb-4">Using innerHTML</h2>
                        <p class="text-gray-700 mb-4">
                           To access an HTML element, JavaScript can use the document.getElementById(id) method.
The id attribute defines the HTML element. The innerHTML property defines the HTML content:
                           </p>
                        <div id="code-block">
        <pre>
&lt;!DOCTYPE html&gt;
&lt;html&gt;
&lt;body&gt;

&lt;h1&gt;My First Web Page&lt;/h1&gt;
&lt;p&gt;My First Paragraph&lt;/p&gt;

&lt;p id="demo"&gt;&lt;/p&gt;

&lt;script&gt;
<span class="keyword">document.getElementById(<span class="string">"demo"</span>).innerHTML = 5 + 6;</span>
&lt;/script&gt;

&lt;/body&gt;
  </pre>
    </div>
    <br>
     <button id="copy-button" class="absolute bg-blue-600 text-white py-1 px-3 rounded hover:bg-blue-500 focus:outline-none focus:ring focus:ring-blue-300">
                Copy
            </button>

                    </div>
<br>
                    <!-- Concept 2: Basic HTML Structure -->
                   <div class="mb-8">
                        <h2 class="text-2xl font-semibold mb-4">Using document.write()</h2>
                        <p class="text-gray-700 mb-4">
                                For testing purposes, it is convenient to use document.write():
                    </p>
<br>

                       <div id="code-block1">
        <pre>
            &lt;!DOCTYPE html&gt;
&lt;html&gt;
&lt;body&gt;

&lt;h2&gt;My First Web Page&lt;/h2&gt;
&lt;p&gt;My first paragraph.&lt;/p&gt;

&lt;p&gt;Never call document.write after the document has finished loading.
It will overwrite the whole document.&lt;/p&gt;

&lt;script&gt;
<span class="keyword">document.write(5 + 6);</span>
&lt;/script&gt;

&lt;/body&gt;
&lt;/html&gt; 

                                 
     </pre>
    </div>
    <br>
     <button id="copy-button1" class="absolute bg-blue-600 text-white py-1 px-3 rounded hover:bg-blue-500 focus:outline-none focus:ring focus:ring-blue-300">
                Copy
            </button>

                    </div>
<br>
                </section>


            </div>
        </div>
    </div>

     <!-- JavaScript to Handle Sidebar Toggle and HTML Editor -->
     <script>
   document.getElementById('copy-button').addEventListener('click', function () {
            const codeBlock = document.getElementById('code-block').innerText;
            navigator.clipboard.writeText(codeBlock).then(() => {
                alert('Code copied to clipboard!');
            }).catch(err => {
                console.error('Error copying code: ', err);
            });
        });
   document.getElementById('copy-button1').addEventListener('click', function () {
            const codeBlock = document.getElementById('code-block1').innerText;
            navigator.clipboard.writeText(codeBlock).then(() => {
                alert('Code copied to clipboard!');
            }).catch(err => {
                console.error('Error copying code: ', err);
            });
        });
  </script>

    </body>
    </html>